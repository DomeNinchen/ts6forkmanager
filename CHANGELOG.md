# Changelog

## Summary

This fork exists because upstream had a persistent video/audio streaming stutter that was never resolved, plus a pile of security findings nobody had triaged. Here's what's different, at a high level — see [Individual Changes](#individual-changes) below for the full, itemized history.

### Video Streaming: Stutter Fixed, Quality Improved
- Root-caused and fixed the stutter — three separate issues: a missing shared volume between the backend and the sidecar container, a single bad RTP timestamp poisoning the adaptive A/V-sync estimate, and VP8 encoding that ran effectively single-threaded regardless of available CPU cores
- Videos are pre-downloaded and streamed from disk instead of feeding ffmpeg a live, rate-limited CDN URL — real HD quality instead of whatever single combined format YouTube happens to offer live (often capped at 360p, or not offered at all). Ported from [uniskela/ts6-manager](https://github.com/uniskela/ts6-manager), itself adapted from [uniplayer1/ts6-manager](https://github.com/uniplayer1/ts6-manager) — credit where it's due
- Multi-threaded VP8 encoding tuned for quality-per-bit, with a rate-control buffer that scales to the target bitrate
- A/V sync pacing is clamped so a single bad timestamp can't stall playback or overflow the RTP queues
- Streams auto-stop when a clip ends instead of looping forever; added a mute/unmute toggle to the in-browser preview

### Security
- [clusterzx/ts6-manager#80](https://github.com/clusterzx/ts6-manager/issues/80) pointed out that a Trivy scan against upstream turned up critical, never-triaged findings — ran the same scan here and fixed what it found (a Go WebRTC dependency chain with 8 critical advisories, multer DoS CVEs, and others)
- Dependabot enabled, plus a manual, on-demand Trivy workflow scanning both the source tree and all three built Docker images
- Branch protection enabled on `main`
- Fixed a real injection surface in the bot flow engine: condition expressions were run through raw string substitution before evaluation, so a crafted chat message could alter what the expression actually checked instead of just being compared as data

### Reliability
- Fixed a Docker signal-handling bug: the backend's `CMD` ran node as a child of a shell (`sh -c "... && node ..."`), so `SIGTERM` on container restart never reached node — it hung for the full shutdown grace period and then got hard-killed, leaving the old SSH query session and music bot connection registered on the TeamSpeak server until *it* eventually timed them out (surfacing as `nickname already in use` / `already member of channel` errors on the next start). Fixed by `exec`-ing into node so it becomes PID 1 and receives the signal directly — restarts now disconnect cleanly and immediately, and the music bot reconnects on its own right after.
- That fix alone wasn't enough: the SSH query client's own `destroy()` call started closing the connection but never waited for it to actually finish before the process exited, so a fast restart could still occasionally race a fresh login against the still-registered old session. Now properly awaits the real SSH close event before shutdown proceeds.
- Also closed a narrower reentrancy gap where a reconnect attempt already in flight at the exact moment of a restart could end up acting on a connection that was simultaneously being torn down.

### Kept Up to Date
Worked through every outdated dependency, easiest to hardest, verifying each with a real container run — not just a successful build — before it shipped:
- Node 20 → 24, Express 4 → 5, Prisma 6 → 7 (backend)
- React 18 → 19, Vite 6 → 8, Tailwind 3 → 4, `react-router-dom` → the unified `react-router` 8, TypeScript 5 → 7, `@tanstack/react-table` 8 → 9 (frontend)
- Removed `zod`, `react-hook-form`, and `@hookform/resolvers` — installed but never actually used anywhere in the codebase

## Individual Changes

Every user-visible or behavior-affecting change, in the order it shipped. See the [merged pull requests](https://github.com/DomeNinchen/ts6forkmanager/pulls?q=is%3Apr+is%3Amerged) for the full commit-level history.

- [clusterzx/ts6-manager#77](https://github.com/clusterzx/ts6-manager/issues/77) asked for adding/removing clients from server groups directly in the UI — the backend and API layer already supported it, just needed the UI: a searchable "Add Member" dialog and a remove button per member on the Server Groups page
- [clusterzx/ts6-manager#57](https://github.com/clusterzx/ts6-manager/issues/57) asked for a way to clear the music queue — already covered here by the dedicated Queue tab on the Music Bots page (clear button, per-item remove, reorder, click-to-play)
- [clusterzx/ts6-manager#39](https://github.com/clusterzx/ts6-manager/pull/39) by [s3bul](https://github.com/s3bul) fixed music chat commands breaking when TeamSpeak auto-wraps links in BBCode (`[URL]...[/URL]`) — adapted here to strip the wrapper once for every command that takes a raw URL, which in this fork is `!play`, `!queue`, and `!stream` (upstream only has the first two)
- [clusterzx/ts6-manager#66](https://github.com/clusterzx/ts6-manager/pull/66) by [ValiOff8](https://github.com/ValiOff8) added the bot's numeric ID next to its name on the Music Bots overview cards — useful when a bot flow's voice-action node needs to reference a specific bot by ID
- [clusterzx/ts6-manager#37](https://github.com/clusterzx/ts6-manager/issues/37) reported that "Test Connection" in Settings always showed success — the backend was already returning a real result, but the UI only checked whether the HTTP request itself succeeded, not the `success` field in the response body. Now checks the actual result and, since the backend also now passes through the real failure reason (e.g. `connect ECONNREFUSED ...`, or the TeamSpeak API's own error message), shows *why* it failed, not just that it did
- [clusterzx/ts6-manager#54](https://github.com/clusterzx/ts6-manager/issues/54) reported an "Unknown escape" error crashing a music bot into its error state — the music-bot protocol parser threw on any TeamSpeak escape sequence it didn't recognize instead of passing it through unchanged, unlike the (correct) SSH/EventBridge-side parser. Now lenient the same way; an unrecognized escape no longer takes the bot down
- [clusterzx/ts6-manager#78](https://github.com/clusterzx/ts6-manager/issues/78) reported that editing a connection's SSH settings has no effect until the connection happens to drop on its own — the code that maintains the SSH-based event/command listeners simply does nothing if one is already open for that server, so it kept running on the old credentials. Editing host/SSH port/username/password now force-reconnects it with the new ones immediately
- [clusterzx/ts6-manager#50](https://github.com/clusterzx/ts6-manager/issues/50) reported that two near-simultaneous token-refresh requests could crash with an unhandled "record not found" error — the rotation logic read, then unconditionally updated and deleted the old token, so a second request racing on the same token could act on a row the first had already removed. It now atomically claims the token first (only one request can win); the loser gets a clean, expected "already used" error instead of a crash
- [clusterzx/ts6-manager#82](https://github.com/clusterzx/ts6-manager/issues/82) pointed out that yt-dlp only ever gets newer on an image rebuild, but YouTube extraction breaks on its own schedule as the site changes, independent of that. The backend container now checks for a yt-dlp update on every start (non-fatal if offline/PyPI is unreachable — verified it gives up after ~12s of retries rather than blocking startup)
- Fixed a bug present in upstream too: the Edit Connection dialog's Update button stayed disabled until a new API key was entered, even though leaving it blank correctly means "keep the existing key" server-side — you couldn't save an edit (e.g. changing SSH credentials) without also re-entering the API key. Now only required when adding a new connection
- [clusterzx/ts6-manager#63](https://github.com/clusterzx/ts6-manager/issues/63) asked for the AFK Mover action to support exempt channel IDs, the same way it already supports exempt group IDs (e.g. a lobby/waiting room where clients should never get auto-moved). Added an "Exempt Channel IDs" field alongside the existing one
- [clusterzx/ts6-manager#46](https://github.com/clusterzx/ts6-manager/issues/46) asked for a command trigger's arguments to be individually addressable instead of one raw string, e.g. `!mute user123 10m spam` → target/duration/reason instead of a single blob you'd have to split yourself in a Condition node. A command trigger's event data now also includes `command_args_list` (indexed, e.g. `{{event.command_args_list.0}}`) and `command_args_length`, alongside the original `command_args` string (kept as-is so existing flows that already use it directly aren't affected)
- [clusterzx/ts6-manager#38](https://github.com/clusterzx/ts6-manager/issues/38) asked for a way to filter the Permissions page down to just the permissions actually set on the selected entity, instead of scrolling past every possible permission most of which are unset. Added an "Only show set" toggle next to the search field
- `!play`, `!queue`, and `!stream` now also accept search terms instead of only a direct URL (e.g. `!play imagine dragons believer`, `!stream some movie title 720p`) — resolved via yt-dlp's own search syntax, so it works the same as a real link, no separate search step or extra UI needed. `!stream`'s optional quality-preset suffix still works alongside a multi-word search query (only the last word is treated as a preset, and only if it's actually one of 480p/720p/1080p)
- `!stop` no longer claims "Playback stopped." when nothing was actually playing (e.g. only a video stream was active — that's stopped separately via `!stopstream`, which `!stop` intentionally doesn't touch)
- [clusterzx/ts6-manager#79](https://github.com/clusterzx/ts6-manager/issues/79) asked for songs already sitting in the music folder (e.g. a volume shared with another app) to show up in the library without a manual upload or YouTube download. Added a "Scan for New Files" button on the Music Bots Library tab, plus the same scan automatically on backend startup — both skip files already known to the library, so re-running is safe
- [clusterzx/ts6-manager#33](https://github.com/clusterzx/ts6-manager/issues/33) — titled "Video Random Playlist" but the actual ask was picking a video already on disk instead of typing its filename or a URL. The Video Stream tab now lists video files already sitting in the music folder and lets you pick one directly as the stream source
- Extended the above: since there was no way to get a video file into the shared folder short of `docker cp`-ing it in by hand, the Library tab now also accepts video uploads (not just audio), with an All/Audio/Video filter to browse either. New uploads and downloads are organized into `music/`/`video/` subfolders (files from before this existed are still found via scan/the stream picker, for backward compatibility)
- Fixed a side effect of the scan feature above: `!play`/`!queue`/`!stream` cache their YouTube download as a bare `<video-id>.<ext>` and never registered it as a library entry, so scan had nothing but the id itself to title it with, showing up as e.g. `zRe4dY4202s` instead of the actual song name. The on-demand "Scan for New Files" button now looks the real title up for these instead, and also fixes any such entry an earlier scan already added under its raw id; the automatic startup scan stays offline/fast and just leaves them for the next manual scan
- The Library list now shows a small "Refreshing..." spinner next to its header while it's updating in the background (e.g. right after a scan or upload finishes) - the list was always refreshing itself automatically, this just makes it visible instead of looking like nothing happened for a moment
- [clusterzx/ts6-manager#58](https://github.com/clusterzx/ts6-manager/issues/58) (partially - bot ID display was already covered by PR #55/#66): SQLite's id counter for a table never resets on its own even after deleting every row, so a re-added radio station or music bot keeps climbing instead of starting back at 1. Settings → Debug now has a "Danger Zone" with a "Reset Radio Station IDs" / "Reset Music Bot IDs" button each, which (as disclaimed in the UI) wipes that table across every server to actually reset the counter, then the next one created starts at #1 again. Radio stations also now show their numeric id next to their name, same as music bots already did
- [clusterzx/ts6-manager#55](https://github.com/clusterzx/ts6-manager/issues/55): a new Settings → Restart tab schedules a periodic restart of ts6-manager's own backend and/or sidecar container (independently selectable, so a setup using only one of the two isn't forced to restart the other) at a configurable time and day-of-week set. This does **not** restart the actual TeamSpeak server - only ts6-manager itself, to clear accumulated connection/bot-manager state or a stuck video-streaming process. Both containers already had a graceful SIGTERM/SIGINT shutdown handler and `restart: unless-stopped`; this just triggers that same shutdown on a schedule instead of only on a real stop/crash
- [clusterzx/ts6-manager#31](https://github.com/clusterzx/ts6-manager/issues/31): the Permissions page's Client picker only ever listed currently-online clients, even though editing by database id already worked for anyone. Added a "Show offline clients" toggle (pulls from the existing but previously-unused-here `clientdblist` query) plus a search box, since a server's full client history can be long — online clients are shown first and marked with a dot
- [clusterzx/ts6-manager#44](https://github.com/clusterzx/ts6-manager/issues/44): `!play`, `!queue`, and `!stream` now also accept a Spotify track link (Spotify doesn't allow third-party apps to actually play through it). The link is resolved to "\<artist> \<title>" via the track's public page — no API key needed — and searched on YouTube the same way a plain-text query already was. Playlist/album links aren't supported, just individual tracks
- A music bot can now be given a custom TS3 avatar and a templated description, editable from the same Create/Edit Music Bot dialog and applied live (no reconnect needed). The description supports placeholders, e.g. `Now playing: {title} ({remaining} left)` — see the Music Bots section under Features in the README for the full placeholder list. It updates on actual changes (song start/change, queue size, editing the template) rather than polling; a template with a time placeholder like `{remaining}` additionally refreshes every 30s while playing so it keeps counting down, and shows a "-"/"0" idle form when nothing is playing instead of going blank. **⚠️ The avatar is currently broken by a TS6 server bug, not by this fork** — see [Known Issues](README.md#known-issues-upstream-ts6-server-bug) in the README. The description template works correctly
- Music bots now identify as a real, TeamSpeak-signed client version (`5.0.0-beta.25`) instead of TS3AudioBot's old placeholder, which showed up as an obviously-fake, far-future build in any client's client-info panel
- Fixed the Bot Flow Engine's Set Variable node: the editor saved the variable's name and value under different config keys (`varName`/`varValue`) than the engine read (`name`/`value`), so every Set Variable node ever built through the UI silently operated on a variable named `""` instead of the name you actually typed, and the value expression was lost the same way. The engine now also accepts the old keys, so flows created before this fix start working immediately without needing to be re-opened and re-saved
- [clusterzx/ts6-manager#48](https://github.com/clusterzx/ts6-manager/issues/48): the Bot Flow engine can now loop. A new Loop node reads an existing list (e.g. a WebQuery node's "Store As" result, such as `clientlist`) and runs its "Body" output once per item, then its "After" output once when done - see the Bot Flow Engine section under Features in the README. **⚠️ Alongside this, WebQuery's "Store As" now stores the FULL result list, not just the first row** - a flow built before this change that reads a single-row command's result (e.g. `serverinfo`/`clientinfo`) via `{{temp.name.field}}` needs one `.0.` added: `{{temp.name.0.field}}`
- [clusterzx/ts6-manager#35](https://github.com/clusterzx/ts6-manager/issues/35): the WebUI now tells you when an update is available, instead of needing to remember to check. Backend, sidecar, and frontend each have their own version now (previously one shared number) - a small banner appears when any of them are behind the corresponding component's `main` branch, naming which one(s) and showing the deploy command to copy. Deliberately notify-only, not a one-click auto-update: actually running `git pull`/rebuilding from inside a container would need Docker socket access mounted in, which is effectively root on the host if that container is ever compromised - not a trade-off to make silently. "Postpone" remembers the version you dismissed and stays quiet about it specifically, but still tells you about the next one after that
- All three components reset to **2.0.0** as their new independent starting point (previously one shared `1.24.x`). Upstream's original commit was itself `1.0.0` and never got a version bump across roughly six months of this fork's most foundational work - the entire video-streaming rewrite (the sidecar didn't exist at all upstream), the security-hardening pass, the widgets, most of the Bot Flow engine - only the last few days of granular per-PR changes were ever actually reflected in a version number (`1.1.0` through `1.24.7`). `2.0.0` is a deliberate, honest reset to where the software actually stands relative to that `1.0.0` ancestor, not a fabricated back-fill of what the number "should" have been at each point along the way
- Prerequisite for [clusterzx/ts6-manager#71](https://github.com/clusterzx/ts6-manager/issues/71) (SSO support): a `viewer` user's TeamSpeak server access could be granted in the database but had no UI or API to actually do it, for any user - local or otherwise. Settings → Users now has a "Server Access" button per `viewer` account to toggle which configured server connections they can see
- [clusterzx/ts6-manager#71](https://github.com/clusterzx/ts6-manager/issues/71): Single Sign-On via any standard OpenID Connect provider (Authentik, Keycloak, Authelia, Zitadel, ...), configurable in Settings → SSO. Local username/password login stays available alongside it. A brand-new user is created as a `viewer` on their first SSO login (matched by the provider's stable `sub` claim, never by email - an email match would let anyone who controls a matching address at *any* IdP "become" an existing account); promote them from Settings → Users afterward if needed. Uses the standard Authorization Code flow with PKCE, state, and nonce - no server-side session or cookie needed for this, since the PKCE verifier and nonce travel as a short-lived signed JWT in the `state` parameter itself, which the IdP echoes back verbatim
- Settings → Update Status now also rechecks automatically on every login (local or SSO), not just every 6h in the background, so what you see right after logging in reflects a fresh check rather than whatever the background timer happened to have cached
- Fixed four pre-built Bot Flow templates in the Template Gallery (Online Counter, Server Stats, Support System, Temp Channel Creator) that were never migrated for the WebQuery `.0.` change noted above - they referenced `{{temp.name.field}}` directly and silently rendered blank values instead of the actual server/client info. Also fixed the Anti-VPN template's detection condition, which could never actually trigger: it compared the VPN-check API's boolean `true`/`false` response against the number `1`, which this engine's strict comparison never treats as equal - VPN users were silently never kicked or banned. And fixed a cosmetic-only bug in the Webhook → Assign Group template's log line, which referenced an unsupported `{{groupId}}` placeholder and always logged it blank
- Added a "Keep played songs" toggle in Settings → YouTube to save disk space. Songs downloaded via chat commands (`!play`/`!queue`/`!stream`) are, by default, eventually picked up into the music library by "Scan for New Files" and kept indefinitely, same as before this setting existed. Turning it off stops that from happening and instead deletes each of these downloads automatically about an hour after they're played. Only affects the ad-hoc chat-download cache - songs added deliberately through the Library tab (upload or "download by URL") are always kept regardless of this setting
- The YouTube Cookies status in Settings previously only checked that a file had been uploaded, not that YouTube still actually accepted it - found the gap chasing down a real `!play` failure ("This video is only available to Music Premium members") that turned out to be caused by the cookies having quietly gone stale (Google/YouTube auth cookies rotate over time) while the status still showed green. Now backed by a real check against YouTube itself (requesting the account's own "Watch Later" playlist, which only resolves when actually logged in), run on a schedule, right after every upload, and on demand via a new "Recheck" button - plus a dismissible banner (same style as the update-available one) that appears app-wide when cookies stop working, so it surfaces before someone hits a failed `!play` in front of everyone
- Two new user roles, alongside the existing Admin/Viewer: **Bot Operator** (viewer permissions plus full control of Bot Flows and Music Bots - including their library, playlists, and radio stations - on whichever servers they're granted access to) and **Music Operator** (the same, but Music Bots only, no Bot Flows). Assign from Settings → Users, same place and same per-server "Server Access" grant mechanism as Viewer already used. Also fixed two related gaps found while building this: Settings → Update Status is now admin-only (previously visible to everyone), and the sidebar's "Management" section (Channels/Clients) no longer shows at all for a non-admin with zero granted servers, instead of leading to a guaranteed "no access" page
- Found testing a fresh SSO login end-to-end against a real IdP for the first time: a viewer with no server access granted yet (the normal state for a brand-new SSO user) saw a misleading "Connection failed - Could not connect to the TeamSpeak server" on the Dashboard, or a bare "No server selected" if the header's server picker couldn't even auto-select a virtual server - both were really the same 403 from the existing per-server access check, just never surfaced as such. Both now show "No access to this server" with a pointer to Settings → Users instead
- The "TS6 Manager" logo in the sidebar is now a link back to the Dashboard, in both the expanded and collapsed sidebar states - previously just static text/icon
- Settings → Update Status's "Recheck Now" button now shows a "Checking for updates..." toast while it runs, followed by "Update available" or "All up to date" - previously gave no feedback at all unless an update happened to be found
- Settings → Users improvements: a "Last Login" column, a search box to filter the list, and an Edit action to rename a user's username or display name after creation (previously only role/status/password/server-access could be changed post-creation)
- Settings → Users: a new "Sessions" action per user lists their active logged-in devices/browsers (by login time) with a per-session Revoke button and a "Revoke All" to force every device to log in again - useful if an account may be compromised, without having to reset the password or disable the account entirely
- An admin can no longer accidentally remove their own admin role, disable their own account, or delete it via Settings → Users - and the same now applies to removing the *last* remaining active admin, even by a different admin. Both are enforced by the API itself, not just hidden in the UI. Previously neither was actually prevented; the UI only hid these controls for whichever account happened to be named literally "admin", which offered no real protection for any other admin account
- Two-factor authentication (TOTP), manageable from Settings → Account: scan a QR code with any standard authenticator app (Google Authenticator, Authy, ...), confirm a code, and get 10 one-time recovery codes shown exactly once for if the device with the app is ever lost. Initially shipped mandatory for local admin accounts; reversed the same day (see below) - 2FA is optional for every account regardless of role. A "remember this device for 15 days" option skips the code on future logins from the same browser once you've entered it correctly once
- Reversed part of the above, same day: on reflection, mandatory 2FA went against how most software actually does this (opt-in is the norm), so local admin accounts are no longer forced through 2FA setup on login - it's just as optional for admins as for every other role now. Nothing else about 2FA changed
- Bot flow actions (e.g. a clock channel renaming itself every hour) always showed up in TeamSpeak's own logs/notifications as "serveradmin" - the same identity your own manual actions through this app use, since both went through the same connection. Settings → Connections now has an optional "Bot Identity" action per server: creates a real, separate ServerQuery login (with the same permissions as the main connection) used only by bot flows going forward, so their actions are attributed to whatever name you give it (e.g. "Hausmeister") instead of blurring together with your own. Nothing changes for any server this isn't set up for
- Fixed the Bot Flow engine's "Send Message" action failing outright with `already member of channel` (TS error 770) when sending to a specific channel: it always moved the query client there first, even when it was already sitting in that channel (the common case, since flow executions for the same bot all share one query connection) or when a concurrent flow execution had just moved it there a moment earlier. Now skips the redundant move when already in the target channel, and tolerates a 770 on the move itself (proceeding to send anyway) without affecting how any other TS error is handled
- Rebuilt the "Welcome Message" bot flow template: the message field is now multi-line, the template picker shows exactly which placeholders it resolves, and it gained three optional pieces - excluding specific server groups entirely (e.g. music bots, other bots), a live "team members online" count from a configurable set of "team" server groups, and a `!wmp ignore` / `!wmp unignore` chat command so a user can opt themselves out (persists across restarts). Two new general-purpose bot-flow actions power this and are usable in any flow: "Count Online in Groups" (counts in one step, deliberately not via a Loop node - looping the online list would cost 1-2 of the engine's shared 500-node-visit budget per person online, which a busy server could exhaust and fail the whole flow) and "List Add/Remove" (add or remove one value from a persistent, named list). Also added a `hasGroup(csv, id)` condition function - exact match against a comma-separated ID list, unlike the existing `contains()`, which does a plain substring match and would wrongly match group "1" against "10" or "21" too
- Fixed two bugs found while live-testing the Welcome Message template above: its multi-line message field was marked as required, but only ever showed placeholder text (never an actual value) until typed into by hand - so "Create Bot" stayed permanently disabled for anyone who left it untouched. It's now genuinely optional, like every other template's message field, and - the second bug - actually falls back to the full example text shown in the placeholder instead of a much shorter one-liner. Also added a third Settings → Debug toggle, "ServerQuery Debug": every WebQuery and SSH ServerQuery command sent to a TeamSpeak server, and its raw response or error, for diagnosing a bot flow action that silently isn't doing what's expected - off by default, same as the existing Voice Bot / Rank Check debug toggles
- Fixed the optional "Bot Identity" feature (see above): its API key was created with scope `write`, which turned out to be too narrow for a lot of ordinary bot-flow actions - anything that just reads server info (e.g. `serverinfo`, used by the Welcome Message template above) failed outright with "out of scope", even though the identity had every permission it needed otherwise. Now created with scope `manage`, matching what TeamSpeak's own quickstart guide uses for general-purpose API keys, so a Bot Identity now genuinely has the same access as the main connection, as originally intended. Settings → Connections → Bot Identity gained a "Fix Permissions" button to reissue the key for a Bot Identity created before this fix, without having to recreate it from scratch or rename it
- New "Private Channel Creator" bot flow template, replacing the old "Temp Channel Creator" - inspired by the "Private Channel Manager" plugin for SinusBot by Smorrebrod / Cedrik Paetz. Gives a client a persistent private channel the first time they join a designated lobby channel, then moves them straight back into that same channel every time after, with them set as its owner (a configurable channel group). Optional pieces: auto-generated channel password, choice of Permanent/Semi-Permanent/Temporary, placing the new channel as a sibling of the lobby or nested under it, and a spam-protection ban for clients who rapidly rejoin the lobby. Building this needed a few small additions to the bot flow engine that any template can now use: the template gallery supports configuration fields that only appear once another field is set a certain way (e.g. the spam-protection options only show up once spam protection itself is turned on); a "Get" operation on the Variable node, reading a stored value into a temp slot for use elsewhere in the flow; a `count(value)` condition function; and action nodes can now have an "error" output, followed instead of aborting the whole flow when the action fails (used here to show a friendly message if channel creation itself fails). Also fixed a real, pre-existing gap this surfaced: a WebQuery list command finding zero matching rows (TeamSpeak's own error 1281) was treated as a hard failure instead of an empty result, which their own WebQuery documentation says to treat as OK
- Fixed the Welcome Message template's online-user count including ServerQuery/bot connections (the bot's own identity, the admin WebUI's own connection, SSH) alongside real users, overcounting how many people were actually online
- Fixed a real gap in the new Private Channel Creator template (see above), found live: a misconfigured "Parent Channel ID" (or any other WebQuery step failing before the channel is actually created - looking up the lobby's own parent, or assigning ownership after creation) aborted the whole flow with no feedback at all, since only the channel-creation step itself had the new "error" output wired up. All three now share the same friendly error message
- The Bot Flow editor's Message and Channel Description fields could only be resized taller, not wider - the side panel they live in is a fixed 256px, too cramped for comfortably editing a multi-line message by hand. Both now have an "expand" button that opens the same text in a large modal to edit instead, synced live back to the normal field
- Fixed a bug found while testing the above: bot flow templates generate their nodes' and edges' ids from two shared counters, one of which (used for edges) didn't actually have its own counter and just reused whichever id the node counter had last handed out. A node with two outgoing edges created back-to-back (e.g. an action's normal and "error" outputs, both wired to the same next step) got the same id for both, which could make the flow editor's canvas misrender one of them. Confirmed this actually happened in the Private Channel Creator template's default setup (three of its error edges each collided with their sibling success edge) - execution itself was never affected, since the engine matches edges by source node and port, not by this id
- New Settings → WebGui tab lets you pick an accent color for the whole interface from a small set of presets (Violet, Teal, Red, Blue, Yellow, Green) - only the brand/interactive color changes, backgrounds and text stay the same. An admin sets the installation-wide default under "Installation Default"; any user (any role) can override it just for themselves under "My Preference", stored in their own browser, without changing what anyone else sees. This is the first step of a larger WebGUI redesign in progress - a full page-by-page visual rebuild is a separate, later effort
- Replaced the placeholder "TS" text badge (sidebar, login screen, browser favicon/tab icon) with the project's own logo, provided by the user
- First real step of the WebGUI redesign ("Command Deck", picked from the direction comparison above): a new deep-navy base theme (replacing the previous slate/charcoal one) with a violet/cyan brand identity - Violet added as a 6th accent preset and the new default for anyone who hasn't picked one; existing installations that already chose a preset keep it unchanged. Headings now use a condensed display typeface (Rajdhani) and body text a humanist one (Manrope), replacing the previous single typeface everywhere; corners are slightly sharper app-wide (smaller default radius). The sidebar's active item now shows a left accent bar in the current brand color. The Dashboard is the first page rebuilt into the new layout language: Online Users, Channels, Uptime and Ping now live together in one unified panel (a dominant arc gauge, driven by the real online/max ratio, alongside compact readouts for the rest - no separate boxes or divider lines), and the Dashboard's main panels get the new angular corner treatment - reserved for this kind of "hero" page only, since data-dense pages (Permissions, Logs, user lists, etc.) deliberately keep the plain, calmer card look for legibility. A new "Bot Flows" summary was also added to the Dashboard - visible to every role for at-a-glance awareness, but only clickable through to the actual editor for a user who has bot-flow permissions. The rest of the app (~16 more pages) is still the old look and will be rebuilt page by page from here. Frontend version reaches **3.0.0**, marking this redesign as its own major milestone, separate from backend/sidecar's own independent version numbers
- Dashboard's Ping reading now measures real network reachability (a timed TCP connect to the server connection's own host) instead of TeamSpeak's own client-averaged ping, which read as a meaningless 0 whenever nobody happened to be online - shows a ~10 minute history sparkline underneath, sampled on the same schedule as the existing Bandwidth chart. A new optional "Dashboard Ping Target" field (Settings → Connections) lets this check point at a different public domain when the connection's own host is an internal/Docker-network address instead; leaving it blank keeps testing the same host as before for every existing connection. Bot flows and every other WebQuery/SSH action are unaffected either way - they always use the connection's real host
- Command Deck's second page: Bot Flows. The list's cards get the same angular corner treatment as the Dashboard's hero panels, and its "Active"/"Inactive" badge now uses the same green/gray as everywhere else that shows this state, instead of the customizable brand accent color. The flow editor's own frame (the palette and its category labels, the outer border, "Node Properties" heading) picked up the new look too - the node canvas itself and its per-type node colors are untouched on purpose
- Command Deck's third page: Settings. Every card across all nine tabs (Connections, Account, WebGui, YouTube, Debug, Restart, SSO, Update Status) now gets the same angular corner treatment, including the Debug tab's red-bordered "Danger Zone" card. The Users tab's actual client table is left as a plain bordered panel on purpose, matching the Dashboard/Bot Flows precedent that dense, tabular pages stay calm rather than getting the "hero" treatment. Frontend reaches **3.2.0**
- Command Deck's fourth page: Virtual Servers. Each server's card gets the same angular corner treatment as the rest of the redesign. Frontend reaches **3.3.0**
- Command Deck's fifth page: Music Bots. The bot player cards and radio station cards get the same angular treatment as the rest of the app's item cards; the standalone panels (the YouTube URL/playlist paste box on the Library tab, and the Video tab's Stream Controls/Live Preview/Viewer cards) get it too, matching how Settings treats its own panels. The list-like containers that hold many rows of data (YouTube search results, a playlist's song list, the queue) are deliberately left plain, same principle as every dense/tabular view elsewhere in the redesign. Frontend reaches **3.4.0**
- Command Deck's angular corner treatment now covers every remaining page in one sweep, per the user's own explicit call to apply it everywhere rather than reserve it for "hero" panels only: Channels (the channel tree), Clients/Bans/Tokens/Messages/Complaints (the shared data table component itself, so every page built on it picks this up automatically), Server Groups, Channel Groups, Permissions, Files, Instance, Server Logs, Music Request History, and the Login/first-time-setup screens. Also retrofitted onto the few spots earlier pages had deliberately left plain (Settings' Users table, Music Bots' YouTube-results/playlist-detail/queue panels), since the "dense views stay calm" distinction from earlier in the redesign no longer applies - every panel gets the same look now. Also fixed a real, pre-existing layout bug this surfaced on Music Request History: a broken `h-full`/`flex-1` height chain collapsed its container to 2px tall, hiding its border entirely - replaced with a fixed-height panel matching the convention already used elsewhere (Channels, Server Groups, Files). Frontend reaches **3.5.0**
- Virtual Servers gained a "Server Counter" summary (total servers as online+offline, how many are set to autostart, total slots, and total autostart slots - the last one omitted entirely if any autostart-enabled server currently has no determinable slot count, rather than showing an inaccurate partial sum) and a "Create Server" dialog (name, slots, port, autostart) - both backed by `servercreate`/`serverlist`, which the backend and API layer already fully supported, just without any UI for it. Also new: a "Statistics" page (always in the sidebar; shows a "select a server under Virtual Servers first" prompt when none is selected) with the currently-selected virtual server's general info (name, status, port, autostart, uptime, created date), connection counts, and network/traffic stats (packet loss, ping, current bandwidth, total traffic since start) - a deliberately reduced first version rather than a full replica of every stat a tool like YaTQA shows, per the user's own call to scope it down to what's easy to build well. Frontend reaches **3.6.0**
- The sidebar's nav sections (Overview, Management, Security, Content, System, Automation) can now be individually collapsed/expanded by clicking their header, independent of the existing whole-sidebar expand/collapse toggle. Each section's state persists across reloads; collapsing the whole sidebar into icon-only mode still shows every icon regardless of any section's collapsed state. Frontend reaches **3.7.0**
- New "Miscellaneous" page (always in the sidebar, same "select a server first" gate as Statistics), inspired by YaTQA's own tab of the same name but scoped down to what's actually useful here: a Global Message tool (broadcast anonymously to every virtual server on the instance, or send to just the selected one under your own query identity); an Identity section to rename this connection's own query nickname - separate from the existing per-server Bot Identity, which only covers bot-flow actions, so a manual action taken through the WebUI itself now no longer has to show as "serveradmin"; Snapshots (create and download the selected server's settings/groups/permissions as a file, or restore one back, overwriting the selected server); and a Danger Zone to reset all permissions on the selected server back to the default template groups. Left out on purpose: WYSIWYG message formatting (a plain text field instead), the desktop-client-only "Location"/cache-folder concept, full channel file backup/restore (a materially bigger feature using the file-transfer protocol rather than a single command - deferred), and a few YaTQA extras that don't apply here (changing your login name/password, and a set of joke/troll features). Real bug caught and fixed while building this: `clientupdate` (used for the Identity rename) fails with "invalid serverID" when run without a virtual server context, same as `whoami` - both need a real selected server, which also explains a long-standing, previously-silent warning from the existing Bot Identity feature's own nickname-setting call. Backend reaches **2.15.0**, frontend **3.8.0**
- Virtual Servers cards gained an explicit Select/Deselect toggle (separate from the header's own server picker) - selecting highlights the card and shows a "SELECTED" badge. The rest of the app still always needs a real selection to function (every other page's own "select a server first" prompt is unchanged), so deselecting one immediately falls back to auto-selecting a server again rather than leaving the app without one - the toggle is there for symmetry/feedback, not to leave the app in a stuck "nothing selected" state. Frontend reaches **3.9.0**
- Statistics is now two tabs instead of one page: "Overview" (instance-wide - servers online/total, users, channels, current bandwidth, and data/packets/files transferred since start, all sourced from the same `hostinfo` call the Instance page already uses, so no new backend work was needed) and "Selected Server" (the existing per-server detail view from the previous release, unchanged). Frontend reaches **3.10.0**
- Settings → WebGui gained a second, independent "Base Theme" choice alongside the existing accent color: the default Command Deck navy, or a new OLED-Black option (a true `#000` page background with cards/surfaces lifted just enough to stay legible - real power savings on OLED screens, fully neutral rather than navy-tinted). Same install-default-plus-personal-override pattern as the accent presets, and fully independent of them - any accent color combines with either base theme. Backend reaches **2.16.0**, frontend **3.11.0**
- The Bot Flow editor's popup text editor (Message/Channel Description fields) gained a "Show Placeholders" toggle, opening a side panel with the placeholder reference right next to the text you're editing - previously only available via a separate help button elsewhere on the page. Shows the tabs relevant to a plain text field (Event, Time, Variables, Temp, Exec), skipping the Condition-only Filter/Functions tabs, plus a new "Templates" tab surfacing the extra placeholders specific gallery templates (Welcome Message, Private Channel Creator) resolve automatically - previously only shown once, at template-creation time. The main "?" Placeholder Reference button also gains this new Templates tab. Frontend reaches **3.12.0**
- Settings → Miscellaneous → Identity gained a "Home Channel ID" field - purely cosmetic/technical, it just controls which channel the base event connection (used for join/leave/move-triggered flows) moves itself into on connect, with no functional effect either way; leaving it empty keeps the previous behavior (wherever a fresh ServerQuery login lands by default). Deliberately doesn't affect the separate per-channel command-listener connections, which must stay in whichever channel their own Chat Command trigger is configured for. Real bug found and fixed along the way: the Query Identity nickname (and the pre-existing Bot Identity nickname) failed to re-apply with "invalid serverID" on every backend restart/reconnect, the same sid-needs-a-real-virtual-server-context issue fixed for the Identity rename action itself in the previous release - now fixed at the source (`ConnectionPool`) so it doesn't just work once, it survives every reconnect. Backend reaches **2.17.0**, frontend **3.13.0**
- Removed the "Home Channel ID" field again - purely cosmetic with no practical benefit. The nickname-re-apply bug fix from the previous release is unaffected and stays. Backend reaches **2.18.0**, frontend **3.14.0**
- New "Advanced Settings" page (per selected server), covering the native TeamSpeak `virtualserver_*` properties that had no UI anywhere yet: welcome/host message and host banner/button (appearance shown to connecting clients), default server/channel/channel-admin groups (as real dropdowns, not raw IDs), server password, minimum identity security level and client build, codec encryption mode, global weblist visibility, reserved slots, complaint auto-ban thresholds, forced-silence and priority-speaker settings, anti-flood points, per-category event logging toggles, and file-transfer quotas/bandwidth limits. Most of the backend (`serveredit`/`serverinfo` route plus its frontend API wrapper) already existed from earlier work and just needed a UI. Also added a small read-only "IP Bindings" card to the Instance page (voice/query/file-transfer listening addresses) - instance-wide, so it lives there rather than on the new per-server page. Two related YaTQA features aren't possible here: TS6's ServerQuery has no license-details command at all, and "default channel delete delay" isn't a documented `serveredit` parameter. Backend reaches **2.19.0**, frontend **3.15.0**
- Fixed a real production deploy bug found from a user-reported log: `Dockerfile.backend`'s startup command ran `prisma db push && prisma db seed || true && exec node dist/index.js` - when `db push` itself failed (e.g. refusing to drop a column with a data-loss warning, which happened for real on a deploy), the trailing `|| true` swallowed that failure too and the backend started anyway against an unmigrated database. `db push` now runs with `--accept-data-loss` (this project has no formal migration review step beyond the PR that changed the schema, so the interactive confirmation prisma would otherwise demand can never be answered here anyway) and its failure is now correctly fatal - only `db seed` failing (e.g. re-running on already-seeded data) is tolerated, via explicit `(... || true)` grouping. Backend reaches **2.20.0**.
- Clients gained multi-select (checkbox per row, "select all" in the header) with a bulk action bar for Move/Kick/Ban/Describe - move/kick/ban genuinely batch into one ServerQuery command each (confirmed live: TeamSpeak's WebQuery API accepts a JSON array for the client-id parameter, e.g. `{ "cid": 5, "clid": [12, 13] }`, moving both in a single call), describe applies the same text to each selected client individually since `clientedit` has no multi-client form. Row selection is now a reusable `DataTable` feature (checkbox column, `getRowId`/`onSelectionChange` props), so the same pattern is available to future bulk-action work on other pages without rebuilding it. First installment of the bulk/multi-select initiative planned alongside the Advanced Settings page. Backend reaches **2.21.0**, frontend **3.16.0**
- Fixed a real bug the user found and root-caused themselves after the SSH-credentials investigation above: editing a server connection in Settings → Connections and saving - even just to rotate the API key, touching nothing SSH-related - silently wiped that connection's SSH username (and functionally broke SSH login even though the encrypted password itself survived, protected by an existing but incomplete guard). Cause: the edit form's SSH User/Password fields always start blank, since the list endpoint never sends the real values back to the browser (only a `hasSshCredentials` flag) - and unlike the API Key and SSH Password fields, SSH Username had no "skip if blank" protection on save, so every save cleared it. Fixed the same way the user pointed out API Key already works: SSH Username is now protected by the same guard, and both SSH fields show a "(unchanged)" placeholder when a connection already has credentials, instead of a plain, misleadingly-fillable-looking blank. Backend reaches **2.22.0**, frontend **3.17.0**
- SSH ServerQuery connections (the base event connection and per-channel command listeners) now send a clean `quit` before disconnecting on backend shutdown, instead of just dropping the transport. Found while root-causing a real TeamSpeak 6 server crash on the user's production vserver ([teamspeak/teamspeak6-server#124](https://github.com/teamspeak/teamspeak6-server/issues/124)): the server logs an abrupt SSH disconnect as `reason 'connection lost'`, and two independent crashdumps showed a heap-corruption crash (`malloc_consolidate(): unaligned fastbin chunk detected`) immediately following exactly that reason - every backend restart carried this risk. Doesn't fix the server-side bug (reported upstream), but avoids triggering it from our side. Verified live: with `virtualserver_log_query` temporarily enabled on the local test server, a real backend shutdown now logs `reason 'disconnecting'` (the same clean reason already used by well-behaved ServerQuery clients) instead of `connection lost`. Backend reaches **2.23.0**.
- Channels page: second installment of the bulk/multi-select initiative. Multi-select (checkbox per row) with a bulk "Edit Selected" action (codec, codec quality, needed talk power, permanent flag - each individually opt-in, so an unchecked field is left alone on every selected channel); "Create Channel" now accepts one name per line to create several at once; each channel also gets a "Use as template" action that reads its full settings (topic, codec, limits, talk power, parent) and opens the create dialog pre-filled under a new name. All three reuse existing `channelcreate`/`channeledit` routes (no batch support in the ServerQuery command itself, unlike the client bulk actions - each selected channel gets its own call). Backend untouched; frontend reaches **3.18.0**
- Server Logs: third installment of the bulk/multi-select initiative (this one isn't about multi-select, but rounds out the same "power-user" backlog list). Added pagination (Newer/Older, using `logview`'s existing `begin_pos` parameter, which the backend already accepted but the page never used), a toggle between the selected server's own log and the instance-wide master log (`logview instance=1`, also already backend-ready), "Add Entry" to write a custom line into the log (`logadd`, admin-only, new route), and Export to `.txt`/`.html` of the currently-filtered view. Also fixed the line-count selector offering 250/500 lines - `logview` only accepts 1-100 per call, so those options silently misbehaved; removed them and added a 25-line option instead. Backend reaches **2.24.0**, frontend **3.19.0**
- Permissions: fourth installment of the bulk/multi-select initiative. Selecting more than one entity in any layer (Server Groups, Channel Groups, Channel, Client) switches the permission editor into "Bulk Apply" mode - the same categorized permission list, but values you set are pushed to every selected entity via `Promise.allSettled`-style looped calls (no native batch support in `servergroupaddperm` etc. either) instead of reflecting any one entity's current state. Deliberately scoped to bulk-edit only, not the two other YaTQA-inspired Permissions features (comparing values side by side, finding every entity with a given permission) - those stay open, separate items for a proper scoping pass later, since they're genuinely different UI concepts, not a bulk-edit variant. Backend untouched (existing per-entity permission routes reused as-is); frontend reaches **3.20.0**
- Files: fifth installment of the bulk/multi-select initiative (not multi-select this time either, but the same power-user backlog list) - a "Move" action per file lets you relocate it to another channel's file repository directly, via `ftrenamefile`'s `tcid` parameter, which TeamSpeak handles entirely server-side with no byte transfer through this app at all. Verified live against the local test server, including confirming the file's actual physical location moved between channels. Doesn't touch the two other YaTQA-inspired file features (image preview without downloading, uploading/downloading whole folder structures) - both would need real byte-level file transfer, which this app has deliberately never implemented (see the existing notice on the Files page itself), so they're treated as declined rather than deferred. Backend reaches **2.25.0**, frontend **3.21.0**
- Permissions gained a "Compare" mode alongside the existing "Bulk Apply" mode when 2+ entities are selected (same layer only - server groups, channel groups, channels, or clients) - a read-only, side-by-side table of each selected entity's current permission values, with "Only show set" (now meaning set on at least one of them) and a new "Only show differing" toggle. Split out from the original bulk-edit item as its own, later to-do at the user's request; scoped down myself once picked up: comparison stays within one layer at a time (matching how selection already works), and it's view-only rather than editable inline, since Bulk Apply already covers pushing a value to multiple entities at once - Compare's whole point is visibility, not a second way to write. Sixth installment of the bulk/multi-select initiative. Backend untouched (reuses the existing per-entity permission routes, one call per selected entity); frontend reaches **3.22.0**
- Permissions gained a 5th layer, "Client in Channel" - a client's permission override within one specific channel, the tier TeamSpeak calls channel-client permissions, alongside the existing Server Group/Channel Group/Channel/Client tiers. Unlike those, there's no server-wide command to list every such override, so this layer works differently: pick a channel, then add specific clients to a working set, rather than checking boxes in a pre-populated list. Fully wired into both Bulk Apply and Compare, same as the other 4 tiers. Compare mode is also now directly editable cell-by-cell (each cell changes just that one entity, independent of the others) rather than read-only, correcting the previous release's scoping once actually asked. This layer also doesn't support the Skip/Negate flags the others do (`channelclientaddperm` genuinely has no such parameters, confirmed against the local ServerQuery docs) - those controls are hidden rather than shown-but-nonfunctional. Backend reaches **2.26.0**, frontend **3.23.0**
- Fixed the Permissions page's "Only show set" toggle silently blanking the whole list in plain Bulk Apply mode: the toggle's control is only shown for a single selected entity or in Compare mode (Bulk Apply alone has no per-entity current state to filter against), but its filter still applied whenever the underlying switch happened to be left on from an earlier session - since Bulk Apply's permission list is otherwise unfiltered, this reduced it to nothing, with the singular "No permissions are set on this entity" message and no visible control to turn it back off. Now resets automatically the moment plain Bulk Apply is entered, the same way Compare mode itself already resets when the selection drops below 2. Frontend reaches **3.23.1**
- Two more self-contained pieces of the ongoing YaTQA-manual-inspired Permissions-area rebuild: Tokens gained a "Temporary Passwords" section (create/list/delete, each with a description, an expiry duration, and an optional target channel to auto-join on connect) alongside the existing privilege keys, using the `servertemppassword*` commands which were entirely unused until now; and Channel Groups gained a "By Client" tab - pick a client, see every channel where they have a non-default channel group, and change it directly from a dropdown. Real TS3 behavior confirmed live while testing the latter: assigning a client the server's own configured *default* channel group makes their override disappear from this view entirely, since TeamSpeak itself only tracks explicit non-default overrides - not a bug, just correctly reflecting that they're no longer overriding anything. Backend reaches **2.27.0**, frontend **3.24.0**
- Server Groups and Channel Groups both gained multi-select (checkbox per row) with a "Delete Selected" bulk action, part of Group Overview from the YaTQA-manual sequence - both pages previously only supported deleting one group at a time. Channel Groups also picked up a "Create Group" button and dialog along the way, since it had no create *or* delete UI at all before this (the backend/API support already existed, just unused) - deleting groups in bulk without any way to create one felt like a gap worth closing at the same time, not a separate ask. Group Overview's other piece, Import/Export between servers, is not part of this release - it has real open design questions not yet asked about. Backend untouched; frontend reaches **3.25.0**
- Group Overview's remaining piece: Server Groups and Channel Groups both gained Export/Import, completing the YaTQA-manual "Group Overview" item. Export uses the bulk multi-select checkboxes already added in the previous release - select one or more groups, download a JSON file containing each group's name and full permission set (deliberately permissions only, not membership, since a target server's clients are usually a different set of people entirely). Import reads that same file back and creates a brand new group (never overwrites an existing one, even on a name collision, to avoid any silent-overwrite risk) with the same permissions. Scoped down to file-based export/import only after asking - not a direct live copy to another configured server connection, which would've needed both ends reachable at the same time for no real benefit over a file. Backend untouched (reuses the existing create-group and add-permission routes, looped per group/permission, the same way the rest of this app's own bulk actions already work); frontend reaches **3.26.0**
- Tokens gained a "Create Token" dialog (privilege keys) - a real gap the user caught: the backend/API support for creating one already existed, but no page anywhere in the app actually exposed it, so there was genuinely no way to create a privilege key at all before this. Pick Server Group or Channel Group, the group itself (and, for a channel group, which channel it applies to), an optional description; the new token is copied to the clipboard automatically since it's a one-time value the admin needs to hand out. Real bug caught and fixed live before shipping: the group picker initially listed every group including template ones (TeamSpeak's own internal defaults, never meant to be assigned directly) and, for server groups, the built-in query groups too - both fail with a real TS3 error if you actually try to create a token for them (confirmed live: "invalid group ID" for templates, "invalid parameter" for query groups). The picker now only lists real, assignable regular groups. Frontend reaches **3.27.0**
